'use client';
import { TeamsPage } from '@/views/teams/ui';

// This is now a simple client component wrapper.
// The actual logic is in TeamsPage.
export default function Teams() {
  return <TeamsPage />;
}
