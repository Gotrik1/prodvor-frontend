
'use server';
/**
 * @fileOverview Generates logo variations for a team based on a description.
 */

// This is a stub implementation to avoid breaking the app.
// The original Genkit implementation is temporarily disabled.

export async function generateTeamLogoVariations(input: { logoDescription: string }) {
  console.log("AI function 'generateTeamLogoVariations' is temporarily disabled.");
  // Return a structure that matches what the frontend expects, but with an error state.
  return {
    logoDataUris: [],
    error: "AI-генератор логотипов временно недоступен.",
  };
}
