
'use server';
/**
 * @fileOverview Generates a news digest.
 */

// This is a stub implementation to avoid breaking the app.
// The original Genkit implementation is temporarily disabled.

export async function generateNewsDigest() {
  console.log("AI function 'generateNewsDigest' is temporarily disabled.");
  // Return a structure that matches what the frontend expects, but with an error state.
  return {
    success: false,
    error: "AI-дайджест временно недоступен.",
    digest: null,
  };
}
